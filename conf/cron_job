# Cronjob für FTP-Upload von Backups
# Dieser Job wird taeglich um 2:00 Uhr ausgeführt.

# FTP-Upload-Cronjob
0 2 * * * root ftp -inv __ftp_server__ <<EOF
user __ftp_user__ __ftp_password__
cd __ftp_path__
mput /home/yunohost.backup/archives/*
quit
EOF
